[install]
# Allow postinstall scripts to run so esbuild can chmod +x its own binary.
# Without this, the esbuild binary lacks execute permissions and OnSpace's
# pre-check fails with "permission denied".
lifecycle-scripts = true
